# eussi

The server for Issue Project Manager

To install dependencies:
```bash
bun install
```

To run "dev" script:
```bash
bun dev
```

To run "start" script (for deployment):
```bash
bun start
```